# Gemini 项目指令

这是一个基于 Rust 的 IPTV 代理项目。在为此项目提供帮助时，请遵循以下准则：

- **语言**：始终使用简体中文与用户交流。
- **编程规范**：遵循 Rust 官方代码风格（使用 `cargo fmt`）。
- **错误处理**：优先使用 `anyhow` 或 `thiserror` 进行错误处理（如果项目中已使用）。
- **测试**：在添加新功能后，确保编写相应的单元测试。
- **文档**：保持代码注释简洁明了，且同样使用简体中文。
